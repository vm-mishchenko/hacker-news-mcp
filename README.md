# Hacker News MCP

## Local development
```shell
# Start MCP server (or run via Intellij)
python src/server.py

# Start inspector (connect to http http://0.0.0.0:8000/mcp)
npx @modelcontextprotocol/inspector

# Programmatically query the server (or run via Intellij)
python src/client.py
```
